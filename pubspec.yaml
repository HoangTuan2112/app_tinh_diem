name: app_tinh_diem
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # Use a more modern SDK constraint

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2  # Keep this, but update if needed.  1.0.2 is probably fine
  http: ^1.2.1          # Updated
  provider: ^6.1.2       # Updated
  shared_preferences: ^2.2.3  # Updated
  collection: ^1.18.0    # Updated

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1    # Updated

flutter:
  uses-material-design: true